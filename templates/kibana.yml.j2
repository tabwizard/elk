server.port: 5601
server.host: "0.0.0.0"
server.name: "Kibana-Wizard"
elasticsearch.hosts: ["http://{{hostvars['el-instance']['ansible_facts']['default_ipv4']['address']}}:9200"]
kibana.index: ".kibana"
logging.quiet: true
